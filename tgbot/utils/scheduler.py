from tgbot.loader import scheduler


def scheduler_jobs():
    # Server time Europe/Moscow
    # scheduler.add_job(send_new_feedback, 'interval', minutes=5)
    # scheduler.add_job(remove_answered_messages, 'cron', hour='1-4')
    # scheduler.add_job(check_compilation_result, 'cron', hour='9, 14', minute=30)
    # scheduler.add_job(send_notifications, 'interval', hours=1, replace_existing=True)
    pass
